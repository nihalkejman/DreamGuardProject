import { StyleSheet } from 'react-native';

const sharedStyles = StyleSheet.create({
  title: {
    fontSize: 18,
    marginBottom: 5,
  },
  contentInput: {
    borderWidth: 1,
    borderColor: 'grey',
    padding: 10,
    width: '100%',
    marginBottom: 10,
  },
});

export default sharedStyles;
